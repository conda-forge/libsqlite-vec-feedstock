[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sqlite-vec"
description = "A vector search SQLite extension."
version = "0.1.9"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "numpy"
]
